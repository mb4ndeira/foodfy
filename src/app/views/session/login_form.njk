<section class="form wrapper">
    <div class="field_container">
        <h3>Email</h3>
        <input type="email" onclick="defaultBorder(event)" placeholder="Digite o email" name="email" class="field" value="{{items.email}}">
    </div>
    <div class="field_container">
        <h3>Senha</h3>
        <input type="password" onclick="defaultBorder(event)" placeholder="Digite sua senha" name="password" class="field">
    </div>
</section>